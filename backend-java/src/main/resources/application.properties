# Server Configuration
server.port=5000
server.servlet.context-path=/api

# Database Configuration (Supabase PostgreSQL)
# Direct PostgreSQL URL: postgresql://postgres:XkFzk8kfL2R6xDkH@db.fhttozvplleogncrjjze.supabase.co:5432/postgres
# Spring Boot JDBC format (used below):
spring.datasource.url=[YOUR SUPA URL]
spring.datasource.username=[]
spring.datasource.password=[YOUR PASS]
spring.datasource.driver-class-name=[]

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Supabase Configuration
supabase.url=[]
supabase.anon.key=[YOUR KEY]

# CORS Configuration
cors.allowed.origins=http://localhost:8000,http://localhost:3000

# Logging
logging.level.root=INFO
logging.level.com.digitalelectronics=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Application Info
spring.application.name=Digital Electronics Quiz API
